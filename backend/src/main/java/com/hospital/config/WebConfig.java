package com.hospital.config;

import org.springframework.context.annotation.Configuration;

/**
 * CORS se configura en {@link SecurityConfig} junto con la validación JWT de Cognito.
 * Esta clase se mantiene vacía a propósito para no duplicar reglas.
 */
@Configuration
public class WebConfig {
}
